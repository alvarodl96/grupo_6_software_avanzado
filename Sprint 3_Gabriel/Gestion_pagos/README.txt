Tenemos dos carpetas:
En la carpeta llamada "Base de datos" tenemos la base de datos utilizada para este microservicio

En la carpeta llamada "API Rest" tenemos el fichero .yaml usado para hacer el API Rest

En la carpeta llamada "python_flask_server" se encuentra todo el server generado por swagger